<h1>Travel</h1>

<ul id="sites">
  {{#each trips}}
    <li>
      <img src="/images/{{image}}" alt="{{name}}">

      <h2>{{name}}</h2>

      {{#each summary}}
        <p>{{this}}</p>
      {{/each}}
    </li>
  {{/each}}
</ul>
