<h1>{{title}}</h1>

<ul>
  {{#each trips}}
    <li>
      <strong>{{name}}</strong><br>
      {{summary}}<br>

      {{#if image}}
        <img src="/{{image}}" alt="{{name}}" style="max-width:200px; margin-top:10px;">
      {{/if}}
    </li>
    <br>
  {{/each}}
</ul>
